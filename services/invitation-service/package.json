{
  "name": "@tempsdarret/invitation-service",
  "version": "1.0.0",
  "description": "Invite service for Temps D'arrêt Studio - handles invitations and magic link authentication (ADR-003)",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && tsc-alias",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/services --passWithNoTests",
    "test:contract": "vitest run tests/contract --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "check": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test:component": "vitest run tests/component --passWithNoTests",
    "test:integration": "vitest run tests/integration --passWithNoTests"
  },
  "dependencies": {
    "@tempsdarret/events": "workspace:*",
    "@tempsdarret/shared": "workspace:*",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@vitest/coverage-v8": "^3.2.7",
    "mongodb-memory-server": "^10.4.3",
    "testcontainers": "^10.28.0",
    "tsc-alias": "^1.9.4",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "invitation-service",
    "magic-links",
    "authentication",
    "adr-003"
  ]
}
