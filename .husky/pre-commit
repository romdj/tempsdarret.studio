#!/bin/sh
# Pre-commit quality gates
# Runs linting and type checking on staged files

echo "🔍 Running pre-commit quality checks..."

# Run lint - fail commit if linting fails
npm run lint

echo "✅ Pre-commit checks passed!"
