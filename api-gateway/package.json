{
  "name": "@tempsdarret/api-gateway",
  "version": "0.1.0",
  "description": "API Gateway and Backend for Frontend (BFF) for photography platform",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "echo 'No source files to lint yet'",
    "check": "tsc --noEmit"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}